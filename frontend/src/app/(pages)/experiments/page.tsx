import ExperimentWorkbench from "@/components/experiment-workbench";

/**
 * Presents the backend-configured experiment workspace.
 *
 * @returns The server-rendered experiments route containing the workbench.
 */
export default function ExperimentsPage() {
  return <ExperimentWorkbench />;
}
