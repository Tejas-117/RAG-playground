import DatasetWorkbench from "@/components/dataset-workbench";

/**
 * Presents the evaluation-dataset management workspace.
 *
 * @returns The server-rendered datasets route containing the connected workbench.
 */
export default function DatasetsPage() {
  return <DatasetWorkbench />;
}
