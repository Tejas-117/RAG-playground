import IndexWorkbench from "@/components/index-workbench";

/**
 * Presents the workspace for configuring a reusable prepared index.
 *
 * @returns The server-rendered indexes route containing the index workbench.
 */
export default function IndexesPage() {
  return <IndexWorkbench />;
}
