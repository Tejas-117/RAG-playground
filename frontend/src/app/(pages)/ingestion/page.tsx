import IngestionWorkbench from "@/components/ingestion-workbench";

/**
 * Presents the corpus-management workspace and its document upload flow.
 *
 * @returns The server-rendered ingestion route containing the interactive workspace.
 */
export default function IngestionPage() {
  return <IngestionWorkbench />;
}
